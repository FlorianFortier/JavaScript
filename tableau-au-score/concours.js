let scoreCible = prompt('entrez le score cible')
let nbEquipe = prompt('veuillez entrez le nombre déquipes')
let compteur = 0
let tours = 0
for (scoreEquipe = 0; compteur <= nbEquipe; compteur++) {
    prompt('Score obtenu par équipe'+ scoreEquipe)
    for (i = 0; i < scoreEquipe.length; i++) {
        if (scoreEquipe[i])
    }
}